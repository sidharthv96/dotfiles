-- -- Pop your lips to lock the mac
-- local function handler(d)
--     if d == 3 then
--         hs.caffeinate.lockScreen()
--     end

-- end

-- noise = hs.noises.new(handler);
-- listner = noise:start()